#!/usr/bin/env bash

# fail if inputs don't exist
set -eu

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

docker-compose -f ${DIR}/docker-compose.yml run pix2face python /pix2face_super/scripts/estimate_coeffs_example.py
